# TUDAO Node Pass - Production Environment Configuration
# Base Mainnet (Chain ID: 8453)
# Last Updated: October 29, 2025

# ============================================================================
# NETWORK CONFIGURATION (Base Mainnet)
# ============================================================================
VITE_CHAIN_ID=8453
VITE_NETWORK_NAME=base

# ============================================================================
# NFT SMART CONTRACTS (Deploy to Base Mainnet)
# ============================================================================
# Deploy contracts using Thirdweb Dashboard or CLI
# Then verify on BaseScan: https://basescan.org
VITE_CONTRACT_VERIFIER=0x...  # Replace with deployed Verifier contract address
VITE_CONTRACT_PROFESSIONAL=0x...  # Replace with deployed Professional contract address
VITE_CONTRACT_FOUNDER=0x...  # Replace with deployed Founder contract address (300 cap)

# ============================================================================
# STRIPE PAYMENT INTEGRATION (LIVE MODE)
# ============================================================================
# Get these from: https://dashboard.stripe.com/apikeys (Live mode)
STRIPE_SECRET_KEY=sk_live_...  # Server-side secret key (NEVER commit to Git)
VITE_STRIPE_PUBLIC_KEY=pk_live_...  # Frontend publishable key

# Webhook endpoint: https://yourdomain.com/api/webhooks/stripe
# Get secret from: https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_...  # Webhook signing secret

# ============================================================================
# THIRDWEB CONFIGURATION
# ============================================================================
# Get from: https://thirdweb.com/dashboard/settings
VITE_THIRDWEB_CLIENT_ID=...  # Client ID (public, safe to expose)
THIRDWEB_SECRET_KEY=...  # Secret key (server-side only, NEVER expose)

# Minting wallet private key - SECURE THIS CAREFULLY
# Recommendation: Use Gnosis Safe multisig instead of single private key
# For now, use a dedicated minting wallet (not your personal wallet!)
THIRDWEB_MINTER_PRIVATE_KEY=0x...  # Private key for NFT minting wallet

# ============================================================================
# CRYPTO PAYMENT CONFIGURATION
# ============================================================================
# Treasury wallet to receive USDC payments (Gnosis Safe multisig on Base)
VITE_TREASURY_WALLET=0x1a994806F4d27Ad0209DBa2489549Dd6252ECb77  # Gnosis Safe - CONFIGURED ✓

# USDC contract on Base Mainnet (official address)
VITE_USDC_CONTRACT=0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913

# ============================================================================
# DATABASE (PostgreSQL Production)
# ============================================================================
# Neon production instance with connection pooling
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Auto-populated by Neon/Replit
PGHOST=...
PGPORT=5432
PGUSER=...
PGPASSWORD=...
PGDATABASE=...

# ============================================================================
# SESSION MANAGEMENT
# ============================================================================
SESSION_SECRET=...  # Generate with: openssl rand -base64 32

# ============================================================================
# FEATURE FLAGS
# ============================================================================
# Enable card payments via Stripe in production
VITE_DISABLE_CARD_ONRAMP=false

# Enable email receipts when system is ready
VITE_EMAIL_DISABLED=true  # Set to false when email system is configured

# ============================================================================
# MONITORING & OBSERVABILITY (Optional)
# ============================================================================
# SENTRY_DSN=https://...  # Error tracking
# DATADOG_API_KEY=...  # Infrastructure monitoring
# LOGTAIL_TOKEN=...  # Log aggregation

# ============================================================================
# SECURITY NOTES
# ============================================================================
# 1. NEVER commit this file to Git with real values
# 2. Use Replit Secrets or environment variables for production
# 3. Rotate keys regularly (every 90 days recommended)
# 4. Use Gnosis Safe multisig for treasury and minting wallets
# 5. Enable 2FA on all service accounts (Stripe, Thirdweb, etc.)
# 6. Set up monitoring alerts for failed transactions
# 7. Keep backups of all private keys in secure offline storage

# ============================================================================
# DEPLOYMENT CHECKLIST
# ============================================================================
# [ ] All contracts deployed to Base Mainnet
# [ ] All contracts verified on BaseScan
# [ ] Gnosis Safe multisig created and funded
# [ ] Stripe live mode webhooks configured
# [ ] Database indexes created (see migrations/)
# [ ] Monitoring alerts set up
# [ ] Backup procedures tested
# [ ] Small test transactions completed successfully
# [ ] Security audit completed and issues resolved
# [ ] Team has access to all credentials via secure vault
